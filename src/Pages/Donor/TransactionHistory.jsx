import React from "react";
import SectionTitle from "../../Shared/SectionTitle";

const TransactionHistory = () => {
  return (
    <div>
      <SectionTitle heading={"Transaction History"}></SectionTitle>
    </div>
  );
};

export default TransactionHistory;
