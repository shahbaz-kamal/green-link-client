import React from 'react';
import SectionTitle from '../../Shared/SectionTitle';

const DonateNow = () => {
    return (
        <div>
            <SectionTitle heading={"Donate Now"}></SectionTitle>
        </div>
    );
};

export default DonateNow;