import React from "react";
import SectionTitle from "../../Shared/SectionTitle";

const AvailableEvents = () => {
  return (
    <div>
      <SectionTitle heading={"Available Events"}></SectionTitle>
    </div>
  );
};

export default AvailableEvents;
