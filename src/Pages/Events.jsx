import React from 'react';
import SectionTitle from '../Shared/SectionTitle';

const Events = () => {
    return (
        <div>
            <SectionTitle heading={"Events"}></SectionTitle>
        </div>
    );
};

export default Events;